# A synthetic miRNA expression dataset.

A matrix containing simulated miRNA expression levels for 358 subjects
(rows) and 100 features (columns).

## Usage

``` r
X2
```

## Format

An object of class `matrix` (inherits from `array`) with 358 rows and
100 columns.
