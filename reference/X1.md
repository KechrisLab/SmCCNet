# A synthetic mRNA expression dataset.

A matrix containing simulated mRNA expression levels for 358 subjects
(rows) and 500 features (columns).

## Usage

``` r
X1
```

## Format

An object of class `matrix` (inherits from `array`) with 358 rows and
500 columns.
